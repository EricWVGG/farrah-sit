import { styled } from '@linaria/react'

export const Slideshow = () => <Wrapper>slideshow</Wrapper>

const Wrapper = styled.div`
  width: 100%;
  aspect-ratio: 1.5;

  grid-area: slideshow;
  background: cyan;
`
